import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    container:{
        width: 353,
        height: 48,
        borderColor: '#000000',
        borderRadius: 12,
        backgroundColor: '#FFFFFF',
        padding: 12,
        borderWidth: 2,
    }
})